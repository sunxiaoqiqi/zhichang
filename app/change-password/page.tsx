"use client";
import { useState, type FormEvent } from "react";
export default function ChangePasswordPage(){const [message,setMessage]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const data=new FormData(e.currentTarget);if(data.get("newPassword")!==data.get("confirm")){setMessage("两次输入的新密码不一致");return;}const response=await fetch("/api/auth/change-password",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({oldPassword:data.get("oldPassword"),newPassword:data.get("newPassword")})});const result=await response.json() as {error?:string};if(!response.ok){setMessage(result.error??"修改失败");return;}window.location.href="/";}return <main className="authPage"><form className="authCard" onSubmit={submit}><span className="eyebrow">SECURITY</span><h1>修改密码</h1><p>密码至少 10 位，并同时包含字母和数字。</p><label>原密码<input name="oldPassword" type="password" required /></label><label>新密码<input name="newPassword" type="password" required /></label><label>确认新密码<input name="confirm" type="password" required /></label>{message&&<div className="authError">{message}</div>}<button type="submit">保存新密码 →</button></form></main>}
