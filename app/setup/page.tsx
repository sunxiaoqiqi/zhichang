"use client";
import { useEffect, useState, type FormEvent } from "react";
export default function SetupPage(){const [available,setAvailable]=useState<boolean>();const [error,setError]=useState("");useEffect(()=>{fetch("/api/setup").then(r=>r.json()).then((v:{available:boolean})=>setAvailable(v.available));},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const data=new FormData(e.currentTarget);if(data.get("password")!==data.get("confirm")){setError("两次密码不一致");return;}const r=await fetch("/api/setup",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({account:data.get("account"),password:data.get("password")})});const v=await r.json() as {error?:string};if(!r.ok){setError(v.error??"初始化失败");return;}location.href="/admin";}if(available===false)return <main className="authPage"><section className="authCard"><h1>系统已初始化</h1><a href="/login">前往登录</a></section></main>;return <main className="authPage"><form className="authCard" onSubmit={submit}><a className="brand" href="/"><span className="brandMark">61</span><span>职场沟通训练营</span></a><span className="eyebrow">FIRST RUN</span><h1>创建首个管理员</h1><p>此入口仅在用户表为空时有效。</p><label>管理员账号<input name="account" required /></label><label>管理员密码<input name="password" type="password" required /></label><label>确认密码<input name="confirm" type="password" required /></label>{error&&<div className="authError">{error}</div>}<button>完成初始化 →</button></form></main>}
